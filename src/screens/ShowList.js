import React from 'react';
import {Text} from 'react-native';
import Center from '../components/Center';
const ShowList = () => {
  return (
    <Center>
      <Text style={{fontSize: 30}}>ShowList</Text>
    </Center>
  );
};

export default ShowList;
